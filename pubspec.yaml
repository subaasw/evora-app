name: evora
description: "Event management and auditorium ticketing app."
publish_to: 'none'
version: 0.1.0+1

environment:
  sdk: ^3.12.1

dependencies:
  flutter:
    sdk: flutter
  go_router: ^17.3.0
  provider: ^6.1.5+1
  fl_chart: ^1.2.0
  google_fonts: ^8.1.0
  qr_flutter: ^4.1.0
  mobile_scanner: ^7.2.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0
  flutter_launcher_icons: ^0.14.4

flutter:
  uses-material-design: true
